#build-web params
OC_USER_ID=$(OC_USER_ID)
NAME=$(HOST_PREFIX)
BUILD_NAMESPACE=$(BUILD_NAMESPACE)
IMAGE_TAG=$(BUILD_TAG)
REPO_NAME=$(SOURCE_REPOSITORY_REF)
REPO_BRANCH=$(SOURCE_REPOSITORY_BRANCH)

#deploy-web params
LABEL_NAME=$(LABEL_NAME)
IMAGE_TAG=$(BUILD_TAG)
NAMESPACE=$(NAMESPACE)

CPU_REQUESTS=1
CPU_LIMITS=2
MEM_REQUESTS=2Gi
MEM_LIMITS=4Gi
MIN_REPLICAS=3
MAX_REPLICAS=5
